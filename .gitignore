# === Entorno virtual ===
.venv/
venv/

# === Variables de entorno ===
.env
.env.*

# === Python ===
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.spec
.pytest_cache/

# === Artefactos generados en runtime (regenerables) ===
# Base vectorial ChromaDB y modelo LDA: se recrean con
#   python scripts/reindex_nexus.py   y   python -m rag.lda_model
localrag/config/data/

# Modelos LLM locales descargados (GGUF, etc.)
*.gguf
*.bin

# === Editores / SO ===
.vscode/
.idea/
.DS_Store
Thumbs.db

# === Temporales ===
*.tmp
*.log

# === Archivos de prueba locales (no son parte del proyecto) ===
localrag/prueba_*

# === Data ajena a PrivaceCheck (no versionar) ===
data_estudio_contable/
CREDENTIALS_DEMO.md

# NOTA: a propósito NO ignoramos *.docx / *.xlsx / *.csv / *.txt / *.pdf.
# El corpus (corpus/synthetic_docs/*.docx), requirements.txt y la documentación
# del proyecto SÍ deben versionarse. Los archivos que sube el usuario por la UI
# van al temp del sistema, no al repo.
